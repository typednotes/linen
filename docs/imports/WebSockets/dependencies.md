# `Hale.WebSockets` module dependencies

Topological order of every module of the `WebSockets` Hackage package as vendored under [`Hale.WebSockets`](../../../../hale/Hale/WebSockets) (derived from `import Hale.*` statements; see [`../module-dependencies.dot`](../module-dependencies.dot) for the full cross-package graph).

An edge **A → B** means *module A imports module B*, so **B must be built before A**.

## Topologically sorted modules

All modules below are ported (or covered by the stdlib) — kept commented out as a completed checklist.

<!-- 1. `Hale.WebSockets.Network.WebSockets.Types` -->
<!-- 2. `Hale.WebSockets.Network.WebSockets.Frame` -->
<!-- 3. `Hale.WebSockets.Network.WebSockets.Connection` -->
<!-- 4. `Hale.WebSockets.Network.WebSockets.Handshake` -->
<!-- 5. `Hale.WebSockets` -->

