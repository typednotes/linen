import Linen.Network.WebApp.Extra.Middleware.RequestSizeLimit.Internal

/-! ### Tests for `Linen.Network.WebApp.Extra.Middleware.RequestSizeLimit.Internal`

    The source module re-exports nothing beyond `RequestSizeLimit` (already
    covered by `RequestSizeLimitTest`); this just checks the empty module
    imports cleanly. -/

#guard true
